# プロジェクトの文脈

このリポジトリは、知識を単文と関係に分解して管理する知識管理システム
`tanbunism` の実装である。Python パッケージ名は `tanbun`、CLI コマンドは
`tb` である。

現在のタスクに関連する場合は、`chatgpt-web/` にある過去の ChatGPT での
議論、設計判断、アイデア、問題意識を探索すること。固定されたキーワード
だけに頼らず、対象コード、機能名、ドメイン概念、その周辺概念から適切な
検索語を導き出すこと。

過去の会話には、完成した結論だけでなく、未整理の思考、試行錯誤、訂正、
未完成のアイデアにも価値があるものとして扱うこと。ただし、それらを
絶対視せず、現在のコードやテストと食い違う場合は、時系列と相違点を
明らかにすること。

# 開発時の注意

- 既存の設計、命名、モジュール境界を確認してから変更すること。
- 振る舞いを変更する場合は、関連するテストを追加または更新すること。
- 検証には原則として Poetry 環境を使い、対象を絞った `poetry run pytest`
  を先に実行すること。必要に応じて `poetry run task lint` とテスト全体も
  実行すること。
- 自動生成物、キャッシュ、仮想環境、`chatgpt-web/` の内容は、明示的な
  指示がない限り変更しないこと。
